# This file tells Git which files and folders to ignore.

# 1. Dependencies
# This is the most important one! node_modules is huge and can
# be recreated just by running 'npm install'.
/node_modules

# 2. Security & Secrets
# Never commit your environment variables file. It contains
# passwords, database links, and API keys.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. Logs
# Log files are specific to your machine and not needed in the repo.
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 4. OS Files
# Files created by your operating system's file browser.
.DS_Store
Thumbs.db

# 5. Build & Distribution
# The "build" or "dist" folder is compiled code, not source code.
/build
/dist